# DESIGN.md — Dockge-Go 视觉系统

> 本文件是界面视觉的唯一事实来源。CSS 与组件若与其漂移，以本文件为准。
> 约束：不引入前端框架与网络字体；布局结构保持与上游 louislam/dockge 一致（主题与双语是允许的偏差）。

## 1. 发现（Discovery）

- **产物类型**：运维工具 / 自托管面板（pro tool，高信息密度可接受，以「扫描」而非「阅读」为主）。
- **使用者**：自建服务、homelab、运维工程师；长时间盯屏，桌面为主、手机快速查看为辅。
- **品牌形容词**：克制（restrained）、精确（precise）、可信（trustworthy）、工程师向（engineering-grade）、安静（quiet）。
- **三词美学essence**：精准 · 硬朗 · 静默（precision instrument panel）。
- **主行动**：快速判断「栈/容器现在什么状态」，其次执行启停与编辑。

## 2. 美学承诺（Aesthetic commitment）

**「仪表盘」方向**：中性灰阶为底、细硬边界（1px）定义结构、**不用弥散阴影**、彩色只用于状态与唯一强调色、等宽字体承载全部元数据与数值。

**Signature move**：**方角状态块 + 等宽大写微标签**——状态用 6px 方角块 + 等宽短标签（替代满屏 pill），区块标题用等宽大写微标签（`STACKS` / `CONTAINERS` / `LOGS`），使界面读起来像仪表而非「卡片墙」。

## 3. 排版

| 角色 | 字体 | 说明 |
|---|---|---|
| 正文 / 标题 | 系统 sans（`ui-sans-serif, system-ui, …`） | 自托管离线可用，零网络请求；标题 600 字重、-0.01em 字距 |
| 元数据 / 数值 / ID / 状态 / 微标签 | `JetBrains Mono, ui-monospace, …` | 等宽对齐；**所有数值用 `font-variant-numeric: tabular-nums`** |

**字号阶（1.2 比例，基准 14px = UI 正文）**

| token | 值 | 用途 |
|---|---|---|
| `--fs-micro` | 11px | 等宽大写微标签（+0.06em 字距） |
| `--fs-meta` | 12px | 次要信息、帮助文本 |
| `--fs-sm` | 13px | 紧凑控件、列表次要列 |
| `--fs-body` | 14px | 正文与控件基准 |
| `--fs-title` | 16px | 卡片标题 |
| `--fs-h1` | 24px | 页面标题（仪表向，比上游 32px 更收敛） |

## 4. 颜色（OKLCH 角色制）

主色沿用上游品牌蓝的色相（连续性），但改为**实色、加深至 AA 对比**，取消渐变（渐变仅保留给「进行中/实时」这类极少数语境，默认不用）。

| 角色 | 明色 | 暗色 | 用途 |
|---|---|---|---|
| `--bg` | `oklch(0.975 0.002 250)` | `oklch(0.18 0.008 250)` | 页面底色 |
| `--surface` | `oklch(1 0 0)` | `oklch(0.22 0.010 250)` | 卡片/浮层 |
| `--inset` | `oklch(0.955 0.004 240)` | `oklch(0.15 0.008 250)` | 输入框/内嵌区 |
| `--border` | `oklch(0.885 0.006 240)` | `oklch(0.30 0.012 250)` | 结构边界 |
| `--text` | `oklch(0.22 0.006 250)` | `oklch(0.84 0.010 250)` | 主文本 |
| `--muted` | `oklch(0.52 0.012 245)` | `oklch(0.62 0.012 250)` | 次要文本（明色对比 ≥4.5:1） |
| `--accent` | `oklch(0.54 0.14 245)` | `oklch(0.72 0.13 250)` | 主按钮/链接/选中 |
| `--accent-fg` | `#fff` | `oklch(0.18 0.01 250)` | 强调色上的文字 |
| `--ok` | `oklch(0.55 0.13 155)` | `oklch(0.70 0.13 155)` | 运行中 |
| `--warn` | `oklch(0.62 0.13 75)` | `oklch(0.78 0.13 80)` | 警告/更新 |
| `--danger` | `oklch(0.53 0.19 25)` | `oklch(0.65 0.17 25)` | 停止/删除 |

分布 60-30-10：中性 60 / 结构（边框、内嵌面）30 / 强调与状态 10。

**暗色是设计出来的，不是反色**：底色近黑非纯黑、文字离白、强调色降饱和、层级靠亮度递进。

## 5. Token 表

- **间距**：4px 基准 —— `4 / 8 / 12 / 16 / 20 / 32`；组内 8、卡片内边距 16、区块间 20、页面外边距 20。
- **圆角**：`--radius` 6px（卡片/输入/按钮）、`--radius-sm` 3px（状态块/小 chip）。**不用 50rem pill**（除语言/主题等极少数圆形元素）。
- **阴影**：卡片**无阴影**，用 1px 边界定义；浮层（下拉/对话框）`0 2px 8px rgba(0,0,0,.14)` + 1px 边界。**禁止「边界 + 弥散大阴影」叠用**。
- **描边**：统一 1px `--border`；聚焦态用 2px accent 外环（`outline-offset: 2px`）。
- **动效**：`--dur-fast` 120ms、`--dur` 160ms，缓动 `cubic-bezier(.2,.8,.2,1)`（ease-out）；只动 `transform/opacity/background-color`；`prefers-reduced-motion: reduce` 时全部禁用。

## 6. 工艺层（Craft layer）

- **布局**：顶栏 1px 分隔线（sticky，无阴影）；侧栏 300px + 内容自适应；`≤991px` 单列（侧栏压缩为受控高度区块、设置菜单转横向 scroll pills）。区块之间用**分隔线与微标签**分组，而不是靠阴影堆叠。
- **组件状态矩阵**（每个交互控件都必须有）：default / hover / active / focus-visible / disabled；危险动作默认描边、确认时实心。
- **按钮分级**：primary（accent 实心）> secondary（描边、透明底）> ghost/icon（无框，hover 才显底）> danger（红色描边，hover 实心）。**按重要性而非颜色语义分级**。
- **表单**：真实标签（不用 placeholder 当标签）；输入 1px 边界 + focus 环；代码类输入用等宽。
- **表格/列表**：文本左对齐、数值右对齐并 tabular-nums；行以 1px 细分隔线；hover 整行高亮；选中 = accent 文本 + 极浅 accent 底（不用渐变）。
- **图标**：lucide 单一系统，stroke 1.75，尺寸 14/16/18；按钮内的图标与文字基线对齐。
- **空/加载/错误态**：空态给一句等宽提示；加载用细进度条或骨架；错误用 danger 文本 + 可重试动作。
- **终端/编辑器**：恒暗控制台面（近黑），是页面的视觉锚点；日志与终端都吃满可用高度。
- **可达性（WCAG 2.2 AA 门禁）**：可见 focus-visible 环、键盘可达、≥24px 点击目标、对比度达标、不靠颜色单独传达信息（状态块配文字）。

## 7. Slop 审计（修正的既有问题）

| 检查项 | 修正前 | 修正后 |
|---|---|---|
| 边界 + 弥散阴影叠用 | 卡片同时有 1px 边界与 `0 15px 70px` 大阴影 | 卡片仅 1px 边界；浮层才用轻阴影 |
| 渐变滥用 | 主按钮、选中项、头像、统计数字皆渐变 | 渐变取消，仅实色 accent |
| pill 满屏 | 按钮/状态/输入框/导航均 50rem 圆角 | 6px 圆角；状态改方角块 |
| 字体系统缺失 | 无字号阶、标题 700、数值非等宽 | 1.2 比例阶、600 字重、全部数值 tabular-nums |
| 间距节奏单一 | 各处 12/14/18/20 混用 | 4px 基准的 6 档节奏 |
| 交互态不全 | 无 focus-visible、disabled 仅降透明 | 全状态矩阵 + 2px focus 环 |
| 暗色为反色 | 直接沿用 GitHub 色板未做层级 | 亮度递进的 surface/inset，强调降饱和 |

## 8. 实测审计（2026-09-19）

| 门禁 | 方法 | 结果 |
|---|---|---|
| 文本对比度 | 浏览器实测渲染色（OKLCH→sRGB 换算 + 透明层 alpha 合成），明/暗 × 首页/设置/栈详情，采样 11 类文本 | **全部 ≥4.5:1**（大字号 ≥3:1）：主文本 16.1 / 微标签 6.81 / 侧栏选中 4.76 / 主按钮 5.69 / 状态块 15.19（暗色：11.52 / 4.76 / 6.14 / 7.62 / 12.05） |
| 焦点可见 | 聚焦 `.btn-secondary` 后读计算样式 | `outline: 2px solid var(--focus)` ✓ |
| 点击目标 | 扫描全部 button/a.btn/.btn-icon/.topbar-pill 的渲染尺寸 | 无 <24px 目标 ✓ |
| 响应式溢出 | 390/768/1024/1440 四档 × 三个页面 | 横向溢出 **0** ✓ |
| 阴影规范 | 遍历 `.card` 计算样式 | 卡片无 box-shadow（仅浮层有）✓ |

修正过程中发现并解决：① 状态块/实时标签原用彩色小字（11px）对比度不足 → 改为**中性文字 + 彩色方块标记**；② 浅色强调色在浅底上 4.07 → 加深至 `oklch(0.50 0.145 245)` 后 4.76；③ 编辑器固定 420px 造成首行与日志卡之间约 240px 死区 → 高度随内容（上游做法），间距降至 30px。

## 9. 变更记录

- 2026-09-19 初版：确立「仪表盘」方向（开发者工具风）、OKLCH 令牌、方角状态块签名、取消渐变与弥散阴影；完成上述实测审计并按结果修正。
